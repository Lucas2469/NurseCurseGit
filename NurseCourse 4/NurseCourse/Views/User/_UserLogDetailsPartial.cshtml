@* @model List<NurseCourse.Models.UserLog>



@if (Model.Count > 0)
{
    <div class="pagetitle">
        <h1><i class="bi bi-clipboard-minus"></i> Log de @Model.First().UserName </h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Inicio</a></li>
                <li class="breadcrumb-item active">Filtros</li>
            </ol>
        </nav>
    </div>

    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Usuario @Model.First().UserName</h5>
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">Fecha</th>
                        <th scope="col">Tipo</th>
                        <th scope="col">Estrategia</th>
                        <th scope="col">Nombre del Cliente</th>
                        <th scope="col">IP</th>
                        <th scope="col">Agente de Usuario</th>
                        <th scope="col">Contador de Inicios de Sesión</th>
                        <th scope="col">Ciudad</th>
                        <th scope="col">País</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var log in Model)
                    {
                        <tr>
                            <td>@log.Date</td>
                            <td>@log.Type</td>
                            <td>@log.Strategy</td>
                            <td>@log.ClientName</td>
                            <td>@log.Ip</td>
                            <td>@log.user_agent</td>
                            <td>@log.LoginsCount</td>
                            <td>@log.CityName</td>
                            <td>@log.CountryName</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    
}
else
{
    <p>No hay logs disponibles para este usuario.</p>
}
 *@